# Production environment variables
# VITE_SERVER_URL is not required as it defaults to window.location.origin
# This ensures the client connects to the same origin where it's served from
# which is necessary for Vercel serverless functions at /api/socket.io
VITE_SERVER_URL=
